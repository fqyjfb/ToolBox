# 更新日志

## v2.8.8

**邮箱聚合插件 OAuth 密钥安全管理**

- 移除源码硬编码的 Google OAuth Client ID / Client Secret，全部改为从环境变量加载，彻底清理仓库明文凭证
- 新增 Outlook OAuth 密钥接入（`OUTLOOK_OAUTH_CLIENT_ID` / `OUTLOOK_OAUTH_CLIENT_SECRET`），与 Google 走同一加载链路
- `emailOAuthService.cjs` 启动时按优先级加载密钥：① 打包内置的 `electron/lib/env-secrets.cjs`；② 项目根目录 `.env`；自带极轻量解析，无需引入 `dotenv`
- 新增构建密钥注入脚本 `scripts/write-env-secrets.cjs`：`electron:build` 前自动按 `process.env` > `.env` 优先级收集 4 个 OAuth 密钥，仅非空时写入 `env-secrets.cjs` 并随 asar 打入安装包
- GitHub Actions 支持 Environments 级密钥管理：`environment: ${{ vars.BUILD_ENVIRONMENT || 'production' }}`，可在 Environment Secrets 中维护 OAuth 密钥，通过 `vars.BUILD_ENVIRONMENT` 自由切换构建环境
- 新增 `.env.example` 模板与 `.gitignore` 规则：`electron/lib/env-secrets.cjs` 永不进仓库，本地开发填写 `.env` 即可使用
- 修复账号管理"通用/店铺/社媒/邮箱/手机号/企业信息/证件信息"列表无法滚动的问题：滚动容器由 `overflow-hidden` 改为 `overflow-y-auto` 并补充 `min-h-0`，滚动仅在面板内部生效，不影响窗口整体布局

## v2.8.7

- 新增邮箱聚合插件主进程支持：IMAP/SMTP 邮件收发、文件夹与邮件列表管理、全文检索、已读/删除/移动标记（基于 imapflow + mailparser + nodemailer）
- 新增邮箱 OAuth2 授权服务：Gmail/Outlook 授权码 + PKCE 授权流、令牌缓存与自动刷新，系统浏览器授权并自动回填邮箱地址
- 新增邮箱 IMAP IDLE 实时推送：新邮件到达即时通知，未读数角标实时同步
- 新增页面切换过渡动画：内容区隐藏滚动条并保留滚动功能

## v2.8.6

- 修复 Web 端（Netlify 部署）新闻/天气接口全部 403 的问题
- 修复 Web 端 HTTPS 页面 Mixed Content 拦截
- 修复手机端底部导航"我的"点击跳转首页的问题
- 修复手机端底部导航栏遮挡所有页面底部内容的问题
- 修复手机端工具中心页面无法滚动、底部工具卡片不可点的问题

## v2.8.5

- 新增离线工具功能：加载用户指定目录下的全部 HTML 文件并以卡片形式展示
- 离线工具卡片点击后在新窗口独立运行对应 HTML 文件，不改动文件本身，保证 HTML 原样正常运行；卡片布局对齐插件商店风格（图标+标题+元信息标签+运行按钮）
- 运行窗口支持浏览器风格右键菜单（剪切/复制/粘贴/全选/复制图片/刷新）
- 离线工具支持目录选择与持久化保存（重启自动恢复）、文件搜索筛选、大小/修改时间展示及列表刷新
- 悬浮球展开/折叠动画：图标从中心以 spring 缓动向外飞散到圆周位置，折叠时同步缩回中心；各图标独立响应鼠标悬浮放大（scale 1.1）并显示 tooltip 标签
- 修复悬浮球展开后整体被放大的问题：禁用 `.expanded:hover` 对球体整体的缩放，hover 放大效果作用于各展开图标自身

## v2.8.3

- 统一页面边距管理：Content 布局容器统一提供内边距，移除工具页面、管理后台、首页等 40 余个页面各自冗余的 p-4/p-6 边距设置，确保三端页面边距一致
- 修复 Select 下拉菜单超出视口右边界被裁剪的问题：靠近窗口右侧时自动左移并限制最大宽度，保证选项完整显示
- 管理员后台布局重构（用户管理/网址管理/工具管理）：头部筛选区与底部分页栏固定不滚动，中间表格区域独立滚动，长列表浏览时分页操作始终可见
- Select 组件下拉菜单宽度改为与触发按钮一致（minWidth 改为 width），视觉对齐更整齐
- Pagination 分页组件间距调整：由上下内边距改为顶部外边距，适配固定底部分页栏布局

## v2.8.2

- 修复记事本列表 Ctrl+C 复制文件到系统剪贴板失败的问题：PowerShell 临时脚本添加 UTF-8 BOM，解决中文路径被按 ANSI 解析成乱码导致复制静默失败
- 修复文件复制提示不准确的问题：剪贴板写入结果真实校验，失败时如实提示"复制失败"，不再假成功；路径检测改用 LiteralPath，兼容含 \[ ] 特殊字符的文件名
- 修复预览区域框选文字按 Ctrl+C 被文件复制抢占的问题：存在文本选区时保留浏览器默认复制行为
- 记事本列表 Ctrl+C 复制提示明确显示文件名称

## v2.8.1

- 新增工具下载功能：管理控制台工具管理数据同步展示，用户端可浏览并下载启用状态的工具
- 新增工具下载独立页面：支持分类筛选、标题搜索、网盘类型徽章展示，下载链接遵循浏览器模式配置打开
- 工具中心页面新增"工具下载"入口卡片
- Web 端头部导航"网址导航"后方新增"工具下载"导航入口，桌面与移动端菜单同步显示
- 面包屑导航补充工具下载页面标签：完整路径显示为"首页 > 工具中心 > 工具下载"
- 优化页面部分UI样式及效果

## v2.8.0

- 统一 Web/手机端 60s 新闻 API 获取逻辑：硬编码同源代理（主备自动切换），桌面端保留设置页更换 API
- 移除首页快捷工具中的"记事本"卡片
- 移除热点新闻页面"懂车帝"分类及 API
- 简化热点新闻刷新按钮加载动画为图标旋转
- 优化桌面端首页新闻错误状态 UI：缩小文字、重新加载按钮改为图标

## v2.7.9

- 修复翻页组件 Select 按钮文本截断问题：移除父级 max-w calc 限制，改用 min-w-max + inline-block w-fit
- 修复翻页组件下拉菜单被裁剪问题：移除下拉容器 overflow-hidden，增加下拉列表最大高度至 max-h-60
- 修复 Web/Mobile 端 textarea 高度塌陷：布局容器由 min-h-screen 改为 h-screen + flex-1 overflow-y-auto
- 修复 Web 端实用工具区未显示：移除 isDesktop 条件包裹，统一工具区渲染
- 修复 Web 端记事本工具卡片出现在实用工具区：newTools 过滤逻辑增加 notes 显式排除
- 工具区区块间距优化：第一个区块添加 mb-6，与云剪贴板页面统一，确保所有选项可滚动选择

## v2.7.8

- 修复翻页组件 Select 下拉菜单被容器 overflow:hidden 遮挡问题：改用 createPortal 渲染到 body，确保下拉菜单完整显示

## v2.7.7

- 网络配置页面"全部恢复默认"按钮移至头部右侧区域
- Web 端首页 UI 重构：去除卡片式风格，添加滚动视差和揭示动画效果
- Web 端头部区域统一高度 42px，缩小搜索框和按钮尺寸
- 修复首页、热点资讯、网址导航页面切换时 LOGO 位置不统一的问题
- 修复搜索框隐藏时仍占据空间导致头部组件位移的问题
- 统一首页、热点资讯、网址导航等页面背景色为白色
- 统一各页面内容区域 padding 为 16px，确保布局一致性
- 隐藏 Web 端所有页面垂直滚动条，保留滚动功能
- 首页搜索组件区域优化，移除冗余分割线样式
- 统一 Select 下拉菜单样式：创建自定义 Select 组件，移除原生 select 边框线，添加圆角效果，保持与输入框样式统一

## v2.7.6

- 账号管理网站/通用/店铺面板新增"便捷登录"右键菜单：打开独立弹窗展示账号信息，支持快捷复制并显示成功状态反馈
- 网络设配置页面UI统一
- 悬浮球切换形象功能：右键菜单问题修复
- 冗余代码清理：删除未使用的导出函数、常量、类型定义及过期 SQL 文档（P1×6）
- 组件合并：合并 Switch/ToggleSwitch、CachedIcon/CachedPluginIcon，消除重复实现（P2-1/2）
- 统一 Bookmark 类型：以 `types/website.ts` 为单一权威源，消除 NavPage 本地重复定义（P2-4）
- ESLint 对齐：补充 `argsIgnorePattern: '^_'`，与 TypeScript `noUnusedParameters` 行为一致
- Web 层图标统一为 SVG：FavoritesBar、Sidebar、NavPage 等页面 `<img>` 引用切换为 `favicon.svg`

## v2.7.5

- 消除网站账号预览时密码字段重复的问题
- 记事本修复对话整理区域子文件夹无法展开的问题
- 修复本地模式下数据隔离失效：查询失败时返回空数据，不再回退云端
- 修复增量同步遗漏本地新增记录问题，同步上传改用批量 upsert 减少网络请求
- 修复存储位置切换与同步并发冲突问题
- 解密异常数据时增加控制台警告，提示非加密格式
- 数据同步限制表级并发数，避免触发 API 限流
- 插件窗口标题和脚本路径添加 XSS 转义，S3 客户端缓存增加上限
- 插件安装失败时自动清理残留目录
- 待办事项组件优化回调依赖减少重渲染，SQLite 写操作添加防抖持久化
- 密码重置增加分类错误提示（频率限制/邮箱未注册）

## v2.7.4

- 记事本新增视频预览播放：支持 MP4/AVI/MOV/MKV/WebM/FLV/WMV 格式，通过自定义协议本地流式播放，无需额外依赖
- 管理员后台网址管理"获取网站信息"API 更换为 oiapi.net
- 首页收藏区域图标加载改用 CachedIcon 组件，与网址导航页面统一，支持 Cache API 缓存
- 设置页面图标缓存管理新增"通用图标缓存"统计展示和单独清理按钮
- 账号管理页面网站账号图标更新为新 SVG 图标

## v2.7.3

- 部分页面样式UI优化调整
- 悬浮球新增切换形象功能：右键菜单备忘录下方添加"切换形象"子菜单，选择后持久化并立即生效
- 删除冗余文件 app.cjs（与 float.cjs 内容重复且未被引用）

## v2.7.2

- 记事本左侧列表新增固定目录功能：支持添加/移除固定目录、拖拽排序、右键菜单操作，点击快速切换目录视图
- 记事本新增独立对话路径设置（💬按钮），对话整理目录与固定目录切换互不影响
- 记事本修复 HTML 文件首次预览空白问题：iframe 添加动态 key 强制内容加载后重建
- 快启动小图标行间距过大：Grid 容器显式设置 gridAutoRows，切换图标尺寸时行高同步更新
- 记事本左侧列表固定目录区域与下方文件列表区域添加分割线
- 修复 CI 构建应用白屏：supabase.ts 改用 Proxy 方案支持运行时重新初始化，避免 .env 缺失时模块加载崩溃
- HotNews API 从 .env 移出，改为代码常量默认值，CI 无需注入该 secrets
- 网络配置页新增 Supabase 和加密密钥配置卡片：桌面端用户可手动填写，保存后立即生效，无需重启
- 移除网络配置页图标缓存冗余的"清理过期"/"清理全部"按钮，统一由存储管理页管理
- 网址导航修复图标加载失败：fetch 受 CORS 限制时回退到 img 标签直接加载
- 网址导航修复图标陆续加载闪烁：loading 时显示默认图标替代脉冲动画
- 网址导航统一缺失图标：移除 Globe SVG，改用 public/网址.png，与首页收藏一致
- 网址导航提取 renderBookmarkCard 函数：消除搜索/收藏/分类三处重复卡片代码（约 200 行）
- 网址导航清理 CSS 重复规则：合并冗余的 .content 样式定义

## v2.7.1

- 快启动拖拽排序优化：改用 rectSortingStrategy 适配网格布局，消除拖拽卡顿和位置偏移
- 快启动图标大小切换：间距随图标尺寸同步，网格列数 auto-fill 自适应，解决数量少时居中且间距过大问题
- 快启动新增图标显示模式切换：右侧添加"仅图标/图标+文字"按钮，支持持久化保存

## v2.7.0

- 首页添加网站导航收藏夹，支持拖拽排序；三端共用 `useHomeFavorites` Hook 统一逻辑
- Web端首页重构布局：天气卡片移至右侧快捷工具上方，统一 section 列间距；搜索框尺寸紧凑化
- CSS 压缩改用 lightningcss，进一步降低 CSS 产物体积
- 运行时性能优化：全量订阅改为选择器订阅，单字段直接选择，多字段使用 useShallow，避免不必要的重渲染

## v2.6.9

- 移除无用依赖 iconv-lite（全项目无引用）
- 修复加密模块 Bug：解密失败时不再静默返回原文，改为抛出异常以确保调用方能感知错误
- 清理死代码：删除未使用的 CountryCodeSearch.tsx 组件和 services/entities/index.ts 桶文件
- 统一 ErrorBoundary 组件：合并重复实现，移除旧版 components/ErrorBoundary.tsx，新组件支持 fallback 属性、重试按钮和错误详情折叠
- 移除未使用的 checkPasswordStrength 导出函数
- 抽取 useDndSensors 公共 Hook：消除 9 个组件中重复的 @dnd-kit 传感器配置代码
- 构建体积优化：修复 manualChunks 分包逻辑，vendor-react 从 4,969 kB 降至 234 kB（-95%）
- 构建体积优化：拆分 vendor-ui（921 kB）、vendor-tools（240 kB）、vendor-viewer（544 kB）等独立 chunk，首屏加载体积大幅降低
- 构建体积优化：显式 externalize Electron 主进程依赖（@aws-sdk、adm-zip、sql.js 等）防止误打包
- 构建体积优化：提升构建目标为 esnext，减少语法转换产物体积
- 移除 82 处低效的 lucide-react 图标 React.lazy 懒加载，改用共享 iconMap 静态导入，消除瀑布式请求
- 移除 PDF Worker 手动导入（2.2 MB），交由 @open-file-viewer/core 内部管理
- 运行时性能优化：6 个组件的 useAuthStore() 全量订阅改为选择器订阅，避免不必要的重渲染
- 运行时性能优化：9 个 img 标签添加 loading="lazy" 原生懒加载
- 启动性能优化：sql.js 改为懒加载，移除启动时 sqliteService.init() 阻塞调用，窗口创建不再等待数据库初始化

## v2.6.8

- 记事本新增 JSON 格式支持（列表图标、编辑、保存）
- 记事本 Ctrl+C/V 复制粘贴改用渲染进程 paste 事件，解决 CF\_HDROP 剪贴板读取失败问题，支持所有格式文件粘贴
- 记事本 Ctrl+C/V 复制粘贴添加目标路径逻辑：优先粘贴到选中文件或文件夹所在目录，未选中则回退根目录
- 记事本文件选择即时显示选中状态，修复点击后延迟选中问题
- 记事本复制粘贴操作添加 Toast 提示反馈
- 记事本左侧文件列表新增拖拽复制功能：支持将文件或文件夹拖拽到目标文件夹实现复制，保留原文件，防止复制到子目录
- 记事本外部拖拽导入文件新增目标路径逻辑：优先导入到选中文件或文件夹所在目录，未选中则回退根目录
- 记事本文件预览升级：统一迁移至 @open-file-viewer 预览引擎，移除旧版 mammoth/xlsx 后端转换代码
- FileViewer 新增中文本地化（locale="zh-CN"）
- 清理冗余依赖与死代码：删除 officePreview\.cjs、convertOfficeToHtml IPC 通道及未使用的 npm 包

## v2.6.7

- 记事本新增 HTML 文件支持：预览/源码双模式切换，iframe sandbox 安全渲染
- 记事本图片预览完善：滚轮缩放（20%-500%）、拖拽平移、缩放百分比显示
- 修复 PDF 文件显示空白：data URL 转 Blob URL 加载，兼容 Electron 沙箱
- 修复 HTML 文件首次预览不加载：iframe 添加 key 属性强制重建
- 修复所有文档容器高度自适应：统一 flex 布局，消除魔法数字
- 记事本左侧列表右键菜单新增"移动"功能：二级菜单显示目标文件夹，支持移动到根目录

## v2.6.6

- 新增网络配置设置页：支持自定义应用更新源、60s 新闻 API、插件商店镜像、图标下载缓存
- 配置空值即默认：用户留空字段自动回退默认值，老用户升级后行为不变
- 复用现有 IPC：仅新增 `network:test` 一个测试连接 IPC，配置读写复用 `get-settings`/`update-setting`
- 模块级缓存优化：`baseApi`、`PluginService`、`getNetworkConfig` 均带结果缓存，配置变更时通过 `setting-changed` 事件自动刷新
- 主窗口 preload 暴露 `clearIconCache`，复用现有 `clear-icon-cache` IPC
- 图标缓存 TTL 与下载超时支持用户配置
- 应用更新检查 API 与仓库主页支持用户配置
- 60s 新闻主源/备源支持用户配置
- 插件商店注册表/Raw/API 镜像支持用户配置，多行 textarea 输入
- 网络配置页提供"测试连接"按钮，仅返回状态码与延迟，不返回响应体（防劫持）
- 网络配置页提供"全部恢复默认"、"清理过期/全部图标缓存"功能（带二次确认）

## v2.6.5

- 修复非管理员账号登录后数据为空且无法添加的问题（account/cloud-clipboard/quick-reply/memo 页面）
- 修复数据同步冲突解决弹窗中保存本地/保存云端按钮无反应的问题
- 清理项目中未使用的依赖和冗余代码
- 修复 Netlify 构建失败问题（pnpm-lock.yaml 不同步）

## v2.6.3

- 修复云剪贴板和快捷回复分类同步问题：移除数据库中不存在的 `order` 字段，避免 Supabase 数据同步报错
- 修复云端同步数据污染防护：在 dataAccessLayer 中添加 `sanitizeForCloud` 方法，自动过滤数据库不存在的字段
- 完善类型定义：移除 `ClipboardCategory` 和 `QuickReplyCategory` 中不存在的 `order` 字段
- 调整云剪贴板和快捷回复分类排序功能为无操作（database schema 不包含 order 列）
- 修复创建/更新分类时不再向云端发送无效字段，确保与数据库 schema 一致
- SQLite 数据库文件按登录用户动态命名（格式：ToolBox\_{用户名}.db），避免同一设备切换账号时数据污染
- 优化用户切换时数据库自动切换机制，登出后重置初始化状态，登录时自动加载用户专属数据库

## v2.6.2

- 修复SQLite迁移后数据同步页面无法切换和设置的问题
- 修复云端模式下数据列表不显示Supabase数据的问题
- 修复存储位置切换后内存状态不同步的问题
- 数据访问层重构：云端模式优先查询Supabase，离线时自动降级SQLite缓存
- 自动初始化SQLite客户端，确保数据库连接正常
- 修复同步数据因JSON反序列化问题导致存储路径丢失和用户关联信息失效的BUG（syncManager.getSyncMetadata未正确解析data字段）
- 修复统计查询结果因数据反序列化问题导致统计值为0的BUG（sqliteService.queryOne用于COUNT查询时异常）
- 修复刷新应用界面时IPC重复注册报错的错误（通过防重入标志避免重复注册）
- 添加存储管理页面"打开存储文件夹"功能按钮

## v2.6.1

- 修复悬浮窗点击插件时先打开主窗口的问题，插件窗口直接启动
- 修复存储管理中插件/应用图标缓存显示0B的问题
- 修复悬浮窗设置插件选择后图标未更新且无法启动的问题
- 记事本页面组件边框样式移除
- 悬浮窗设置页面图标背景色统一
- 插件中心图标添加离线缓存功能
- 悬浮球设置新增已安装插件配置入口
- 插件商店性能优化：已安装插件信息完全通过本地IPC获取，避免无效网络开销；安装/卸载/更新后仅刷新本地列表

## v2.6.0

- OCR插件服务增强：修复端口配置不生效问题，统一从localStorage读取服务配置
- OCR插件状态同步：统一服务状态显示入口到头部，设置面板移除冗余状态UI
- OCR依赖安装进度反馈：实现实时进度条显示，支持安装状态（开始/下载/解压/完成/错误）
- OCR服务管理：新增服务关闭功能，支持服务启停控制
- OCR设置面板精简：移除冗余配置项，仅保留必要配置（端口、超时、Python路径等）
- Electron IPC序列化修复：修复OCR插件IPC通信中"An object could not be cloned"错误
- Python进程编码修复：添加UTF-8编码支持，解决Windows下中文乱码问题
- 网址导航页面优化：调整分类标签样式，增加分类关联功能

## v2.5.10

- 修复OCR插件运行诊断和安装依赖功能报错：解决"An object could not be cloned"序列化错误

## v2.5.9

- UI 主题配色统一：所有按钮颜色统一使用 bg-primary text-button-text，支持浅色/深色主题切换
- 修复深色主题下插件商店图标颜色冲突问题
- 移除工具页面中的硬编码按钮颜色
- 修复 QuickLaunch 页面中硬编码十六进制颜色（#009ebe）

## v2.5.8

- 网址导航页面性能优化：优化图标加载策略，启用浏览器HTTP缓存，减少重复网络请求，修复CachedIcon组件blob URL内存泄漏问题，实现图标请求去重机制，分类关联算法优化，移除NavPage内部双重缓存，统一依赖WebsiteService缓存

## v2.5.7

- 移除AI助手功能：包括聊天界面、历史记录、角色预设页面
- 移除字体生成器功能及相关字体图标资源文件
- 移除设置页面中的Agnes AI设置标签页
- 清理相关路由、服务、状态管理和类型定义文件
- 功能迁移为插件版，在插件商店中安装使用

## v2.5.6

- 备忘录功能增强：卡片内容超链接可点击，按设置中的浏览器模式打开（内部弹窗/外部浏览器）
- 云剪贴板功能增强：超链接打开方式统一遵循设置中的浏览器模式配置
- 超链接复制功能：备忘录和云剪贴板卡片上的超链接旁新增复制按钮，一键复制链接
- 代码质量优化：修复11个ESLint any类型错误和1个依赖数组警告

## v2.5.5

- 插件商店卡片布局优化：调整为紧凑型，缩小内边距、图标尺寸和间距，每行显示4个插件卡片
- 插件描述显示优化：改为单行显示，超出内容自动省略（显示省略号）
- 插件安装功能修复：修复使用镜像源克隆时只生成 .git 目录的问题，浅克隆失败时自动回退到完整克隆
- 移除不可用镜像源：移除 github.com.cnpmjs.org，保留 hub.fastgit.xyz、gh.fastgit.org 和 github.com

## v2.5.4

- 插件商店功能优化：添加多个 GitHub 镜像源支持（github.com.cnpmjs.org、hub.fastgit.xyz、gh.fastgit.org），解决国内网络环境下无法下载安装插件的问题
- 插件下载自动重试机制：当主源下载失败时，自动切换到备用镜像源继续尝试
- 插件注册表和图标资源镜像支持：添加 fastgit.org、gitmirror.com 镜像源，确保插件列表和图标在国内网络正常加载
- OCR插件服务增强：diagnose.py 添加包导入检测，install\_deps.py 添加 --force 参数支持强制重装依赖，ocrIpc.cjs、preload.cjs、ToolPanel.tsx 支持 force 参数传递

## v2.5.3

- 移除OCR文字识别功能（已迁移为插件版）
- 移除python-service目录及相关配置
- 修复OCR插件服务启动失败问题
- 优化OCR插件设置界面为紧凑型

## v2.5.2

- 新增插件商店功能：支持浏览、搜索、安装和管理插件
- 插件商店分类自动从插件注册表生成，支持去重和排序，默认显示"全部"分类
- 插件图标显示优化：优先使用图片图标，图片加载失败时自动切换为SVG图标
- 插件图标资源管理：按插件名称组织到独立文件夹，提升资源管理效率

## v2.5.1

- 文件管理功能优化：移除"文件列表(N个项目)"标签，新增文件类型筛选功能
- 文件管理功能优化：支持按图片、文档、表格、视频、文件夹类型筛选，默认全选
- 文件管理功能优化：新增全选复选框，支持三态显示（全选/部分选中/未选）
- 文件管理功能优化：搜索与类型筛选同时生效，未选中任何类型时显示提示信息

## v2.5.0

- 账号管理页面布局优化：统一头部和内容区域边距为 px-4，与备忘录页面保持一致
- 账号管理页面分页组件优化：移除分割线，减少冗余嵌套层，Pagination 组件与内容区域同级
- Pagination 组件增强：添加 className 属性支持自定义样式

## v2.4.9

- 创建 useCallbackRef 自定义 hook，统一处理函数循环依赖问题
- 优化 memo、AI聊天、记事本聊天功能中的函数引用方式，消除手动 ref 转发样板代码
- 待办事项页面优化：修复已完成任务显示已逾期问题，提取魔法数字为常量，优化弹窗布局和紧凑型UI
- 模态框组件样式统一：头部/底部高度调整，内边距统一为 p-4，底部按钮居中对齐

## v2.4.8

- 设置页面样式优化：创建 SettingCard、SettingSection、SettingRow 基础组件，减少冗余嵌套层，统一卡片样式
- 设置页面左侧导航栏按钮间距修复：添加垂直间距（mb-1）
- 通用账号列表滚动条隐藏：添加 scrollbar-hide 类，保持滚动功能

## v2.4.7

- 热点新闻页面瀑布流布局优化：图文新闻使用多列瀑布流，文字新闻使用列表形式
- 热点新闻页面响应式优化：手机端1列，平板端2列，中屏3列，大屏4列
- 热点新闻页面样式优化：移除卡片边框和阴影，图片按比例完整显示

## v2.4.6

- 更新机制优化：添加完整的7状态机（idle/checking/available/downloading/ready/installing/error）管理更新流程
- 更新机制优化：支持 GitHub Release 302 重定向下载，修复下载失败问题
- 更新机制优化：安装程序启动后自动退出应用，避免文件占用冲突
- 更新机制优化：添加错误分类映射，提供用户友好的错误提示
- 更新机制优化：按钮样式使用 CSS 变量，支持主题切换
- 更新机制优化：修复 UpdateButton 竞态问题，移除 finally 块中对安装状态的错误重置
- 更新机制优化：下载完成后进入 ready 状态，用户确认后再执行安装

## v2.4.5

- 关于页面新增系统信息区域（仅桌面端）：显示操作系统版本、CPU信息、内存使用、计算机名等
- 记事本新增PDF格式预览支持
- 记事本新增.doc格式支持，修复列表不显示和预览乱码问题
- 修复记事本预览区域只能看到首屏的问题：PDF预览设置高度为100vh，Office预览设置minHeight为100%

## v2.4.3

- 网址导航页面性能优化
- 认证状态缓存优化：添加 5 分钟 TTL 过期机制，避免重复调用 supabase.auth.getUser()
- UI 主题一致性修复：RecentsPage / Sidebar / Home Tooltip 硬编码颜色替换为 CSS 变量
- 主题切换开关：Switch 组件颜色提取为专用 CSS 变量，支持深色模式
- LoginPage.css 统一化：圆角、字号、边距接入 Token 体系
- theme.css 清理：删除重复定义的 --space-3、--transition-fast、--transition-normal
- HotNewsPage.css 去重：删除与 theme.css 重复的 .loader/.dot 动画定义
- 清理 float.css 重复文件 styles.css

## v2.4.2

- 记事本功能主题优化：移除所有硬编码颜色，统一使用主题变量（primary、error、success、warning），支持明亮/暗黑主题切换
- 记事本界面优化：对话界面头部标签更新为"快速记录想法，稍后整理"；文档页面保存按钮改为仅图标按钮；空状态"新建笔记"按钮改为主按钮样式
- 文件管理功能优化：支持拖动分隔线调整三栏宽度并持久化；文件列表按类型显示不同图标（代码、图片、音频、视频、文档等），每种文件类型配有对应颜色

## v2.4.1

- 记事本功能优化：右键菜单新增"新建笔记/文件夹"选项；空状态添加快捷入口按钮；清空缓存时保留记事本配置；新增 clearAllExcept 方法支持选择性清除缓存
- 备忘录功能优化：预览弹窗隐藏滚动条并添加编辑按钮；添加/编辑窗口布局优化为水平排列
- 设置页面优化：清除缓存后应用自动重启，确保缓存彻底清除

## v2.4.0

- 新增备忘录功能：支持分类管理、优先级标记、搜索查询、拖拽排序、数据同步
- 备忘录分类支持拖拽排序，与云剪贴板/快捷回复功能保持一致
- 备忘录卡片支持点击预览、复制内容、右键编辑/删除操作
- 备忘录预览弹窗支持悬浮显示复制按钮
- 备忘录页面支持头部搜索框集成，与云剪贴板功能保持一致
- 添加/编辑弹窗支持防误关（点击外部不会关闭）
- 备忘录数据支持云端/本地同步，集成到同步设置页面
- 托盘菜单新增"新建备忘"选项，悬浮球菜单新增"备忘录"选项
- 空状态优化：添加"创建一条"快捷按钮，支持一键创建备忘录

## v2.3.9

- 移除未使用的测试依赖：@testing-library/jest-dom、@testing-library/react、vitest
- 减少 node\_modules 体积
- 优化项目依赖结构，提升安装和构建速度
- 清理 Vite 配置中冗余的 chunk 配置（jspdf、html2canvas）
- 为高频渲染组件添加 React.memo 缓存：Toast、WeatherCard、Sidebar、TodoPage
- 创建 localStorageService 统一封装本地存储操作，提供错误处理和键名管理
- 替换项目中所有直接 localStorage 调用为 localStorageService，实现本地存储统一管理
- 添加全局错误边界组件 ErrorBoundary，提升应用稳定性
- 开发 usePerformanceMonitor 性能监控钩子，用于监控组件渲染性能
- 修复多个 TypeScript 类型错误，确保类型安全
- 快启动功能新增"移除失效"菜单项，自动检测并清理路径无效的应用程序
- 新增 fileExists IPC 接口，支持前端检查文件路径有效性
- 云剪贴板/快捷回复功能分组新增拖拽排序功能，支持通过拖拽调整分类顺序
- ClipboardService 和 QuickReplyService 新增 updateCategoryOrder 方法，支持分类排序持久化

## v2.3.8

- 记事本功能核心修复：修复预览模式切换回编辑模式后内容空白的问题
- 记事本功能网络优化：Vditor 静态资源本地化，支持离线运行，消除网络 I/O 阻塞
- 记事本功能性能优化：移除动态 CSS 注入，改为静态文件，减少不必要的重渲染
- 记事本功能样式优化：CSS 变量化主题切换，支持实时主题变化，无需组件重建
- 构建优化：移除 vite.config.ts 中 marked 和 rehype-raw 死引用
- 设置页面存储管理页面布局优化：移除冗余嵌套层，统一上下边距与通用设置界面一致
- 设置页面左侧导航栏样式优化：折叠按钮移至底部固定位置，移除冗余嵌套，统一按钮尺寸和边距

## v2.3.7

- AI助手聊天界面新增下载按钮（支持下载对话记录）
- AI助手代码块标题栏优化：支持中文语言标识
- 记事本页面代码块标题栏功能：添加标题栏和复制按钮，支持中文标识

## v2.3.6

- AI助手角色预设页面优化：新建/编辑功能改为模态框形式，提供更大编辑空间
- AI助手角色预设页面优化：系统提示词textarea行数增加至10行，提升长文本体验
- AI助手角色预设页面优化：预览模式下系统提示词支持展开/收起，默认显示2行
- AI助手角色预设页面优化：图标与标题水平排列，缩小图标尺寸（w-10→w-8）
- AI助手角色预设页面优化：减少冗余嵌套层，统一缩小内边距和按钮尺寸

## v2.3.5

- AI助手功能优化：修复首次创建对话发送消息后界面无反应的问题
- AI助手角色预设优化：新建对话和历史对话切换时正确应用角色预设，创建/更新角色预设时同步保存到数据库
- AI助手历史记录新增右键菜单：支持重命名、删除对话及折叠/展开侧边栏
- AI助手编辑消息功能完善：编辑消息后重新发送并生成新回复
- AI助手删除对话按钮改为弹出确认对话框
- 统一应用折叠/展开侧边栏按钮图标

## v2.3.4

- 统一管理员后台页面UI风格：页面标准统一边距（p-4）、文字大小和布局结构
- 移除外层容器和冗余嵌套层，简化DOM结构
- 用户管理页面编辑用户功能从独立页面改为项目统一模态框组件（Modal），提升交互体验和代码复用
- 删除独立的编辑用户页面（user-edit.tsx）及对应路由（/admin/users/edit/:id）
- 移除未使用的导入（Database图标、Check图标、useNavigate），优化代码质量

## v2.3.3

- 优化首页启动性能：新闻区域改为按需加载，默认仅加载"热点"新闻，其他分类在用户点击后加载
- 新增天气数据缓存机制：天气数据缓存15分钟，减少重复网络请求，提升加载速度
- 新增 lucide 图标预加载：消除工具网格图标闪烁问题
- 优化 App.tsx 初始化逻辑：认证状态获取改为非阻塞异步方式，缩短白屏时间
- 优化 Electron 窗口配置：添加 backgroundThrottling 和 nativeWindowenpen 选项，提升窗口响应速度
- 首页组件添加 React.memo 缓存：Home、ToolGrid、NewsContainer、MoyuCard 组件减少不必要重渲染
- 构建产物优化：减少首页加载时的网络请求数量约67%
- 设置页面用户体验优化：天气城市输入框改为受控组件，提升交互响应
- 设置页面快捷键管理：新增恢复默认快捷键功能，支持一键重置所有快捷键
- 设置页面OCR配置：诊断和安装模态框统一为应用标准Modal组件，提升UI一致性
- 设置页面快捷键冲突检测：实现实时冲突检测，输入快捷键时即时显示冲突提示，冲突时禁用保存
- 设置页面同步冲突处理：新增同步冲突处理界面，支持选择保留本地或云端版本
- 优化数据同步设置及处理逻辑：新增数据启动同步设置界面，支持选择是否自动同步数据
- 修复数据同步处理逻辑：在数据同步处理过程中，修复了数据同步失败的问题，确保数据同步的稳定性和可靠性

## v2.3.2

- 统一页面布局风格：统一边距为 p-4
- 统一个人信息、关于页面布局风格，移除外层容器和边框线
- 个人信息页面优化：移除所有容器边框和背景色，退出按钮移至标题栏右侧，UI优化

## v2.3.1

- 修复侧边栏显示开关导致页面错误的问题，解决 React Hooks 调用次数不一致的错误
- 锁定界面 UI 美化，采用侧边栏灰色系配色风格
- 在顶部导航栏添加设置按钮作为备用入口，确保侧边栏隐藏后仍可访问设置
- 锁定窗口界面UI及布局重制

## v2.3.0

- 重构桌面端左侧导航栏，采用三区结构设计
- 新增动态导航栏功能：用户可通过工具中心卡片右键菜单添加/移除工具到侧边栏
- 动态导航栏支持拖拽排序，使用 @dnd-kit 库实现，与快启动页面保持一致
- 新增最近使用页面，自动记录用户访问的工具，最多保留 10 条记录，超出自动清理
- 折叠/展开按钮移至右侧头部区域面包屑导航左侧
- 工具中心页面右键菜单新增"加入侧边栏"/"移出侧边栏"选项
- 新增工具中心导航菜单入口
- 修复 useLocation 在 Router 上下文外调用的错误

## v2.2.9

- 移除记事本功能的导出功能（PDF 和 Word）
- 清理导出相关冗余代码，优化构建产物大小

## v2.2.8

- 记事本功能新增导出功能：支持将 Markdown 笔记导出为 PDF 和 Word 文档
- 在左侧文件列表右键菜单中添加"导出"菜单项（包含 PDF 和 Word 子菜单）
- PDF 导出使用 html2canvas + jsPDF 实现，支持标题、粗体、斜体、代码块、列表等格式
- Word 导出使用 docx 库实现，支持标题（H1-H4）、列表、引用、代码块、粗体、斜体等格式
- 记事本功能仅在桌面端显示，Web 端和手机端工具中心隐藏记事本入口
- 导出功能仅在桌面端可用，非桌面端显示"导出功能仅支持桌面端"提示

## v2.2.7

- 优化AI助手页面布局：修复三端（桌面端、Web端、手机端）高度计算，确保页面不产生垂直滚动条
- AI助手左侧侧边栏展开宽度缩小：从210px/240px调整为180px/200px
- 移除AI助手右侧聊天内容与输入组件之间的分割边框线
- 移除AI助手折叠按钮的顶部边线
- 隐藏AI助手聊天内容区域和侧边栏对话列表的垂直滚动条
- 移除AI助手页面所有内联样式，统一使用Tailwind类和CSS变量
- 移除任务列表页面颜色选择器的内联样式
- 工具中心页面卡片尺寸缩小：桌面端最大不超过80px
- 工具中心页面布局边距统一：采用与账号管理页面一致的布局结构（p-6外层边距 + 白色卡片容器）

## v2.2.6

- 新增AI助手功能：支持文字对话、图片生成、视频生成、字体生成、角色预设，数据自动同步本地与云端存储
- 手机端和web端数据页面添加搜索支持，搜索框位于头部区域
- Web端天气卡片位置调整到搜索区域右上角，优化尺寸和布局
- 天气页面手机端响应式优化：生活指数和天气概况改为垂直排列，避免重叠

## v2.2.5

- 优化 Web 端和手机端页面
- 配置 Vite 代理解决开发环境 CORS 问题，新闻 API 请求通过代理路径 `/api/news` 转发
- 在 theme.css 中添加缺失的 CSS 变量 `--space-9` 和 `--space-10`

## v2.2.4

- 新增记事本聊天功能，支持对话模式切换，在左侧笔记列表点击"对话"进入聊天界面
- 消息支持发送、标记完成、多选操作，按日期分组展示
- 支持将消息移动到多种目标文件：稍后阅读、阅读清单、观影清单、购物清单、日记本、归档
- 聊天记录文件命名为中文（对话.md），自动按日期生成分组标题

## v2.2.3

- 修复右键菜单二级菜单位置问题，新增智能边界检测确保菜单完整显示
- 优化右键菜单样式，采用紧凑型设计
- 新增窗口大小响应，使用 ResizeObserver 动态调整二级菜单位置
- 提升代码质量，添加防御性编程和资源清理
- 优化数据同步，新增启动时静默同步和增量检查功能

## v2.2.2

- 修复自动锁定功能，使用 Windows API 获取系统空闲时间，确保前台后台都能正确检测用户活动
- 优化系统活动检测策略，动态调整检测间隔，减少 CPU 消耗
- 清理自动锁定相关冗余代码，移除窗口级别活动监听
- 移除重复依赖 @electron/rebuild（electron-builder 已内置）
- Account Panel 组件错误日志统一使用 loggerService
- 修复快启动拖拽功能，使用 Electron webutils.getPathForFile() 方法正确获取文件路径
- 修复日志窗口滚动条问题，采用更紧凑的布局设计
- 删除 electron/logs 目录下未使用的冗余文件，将日志页面从 src/pages/tools/logs 移动到 src/pages/logs

## v2.2.1

- 创建 ProtectedRoute 组件和 renderRoutes 函数，消除约150行重复路由代码
- 待办事项页面搜索输入添加 300ms 防抖处理，减少频繁数据库查询
- 创建通用 CRUD 服务基类 BaseService，减少约40%服务层代码
- Toast 组件从 styled-components 迁移到 Tailwind CSS
- 添加工具页面预加载策略，高频工具在浏览器空闲时自动预加载
- 移除加密密钥硬编码，添加密钥验证机制，强制要求配置环境变量
- 新增自动锁定功能，支持设置多种时长，鼠标键盘停止活动后自动锁定应用

## v2.2.0

- 新增锁定功能，支持通过 Ctrl+L 快捷键锁定/解锁应用，托盘菜单集成，密码安全

## v2.1.10

- 重构数据访问层，统一本地/云端存储接口，确保存储模式切换正常工作
- 修复多个 TypeScript 类型错误，确保类型安全

## v2.1.9

- 云端模式下数据增删改自动同步到本地存储，确保存储管理页面统计数据实时更新
- 待办事项、快捷回复、云剪贴板、网站、通用、店铺、社媒、邮箱、手机号、企业信息、证件信息的增删改操作均自动同步到本地存储

## v2.1.8

- 新增存储模式切换功能（本地/云端），默认设置为云端模式
- 云端模式下数据增删改自动同步到本地存储，确保存储管理页面统计数据实时更新
- 账号管理页面添加平台可见性设置功能，支持启用/禁用各账号分类显示
- 实现账号管理 Tab 拖拽排序功能
- 存储管理页面统计卡片根据平台可见性设置自动过滤显示

## v2.1.7

- 新增基于 IndexedDB 的本地数据持久化，支持离线优先架构
- 新增数据同步模块，实现双向同步、冲突处理、增量同步（基于 updated\_at 时间戳）
- 新增同步设置页面，提供同步开关、模块管理、存储管理等功能
- 使用 AES-CBC 加密算法保护敏感数据，格式为 iv:encryptedData（base64编码）
- 支持用户切换时自动清理旧数据，确保数据安全性
- 离线状态下的操作自动缓存，联网后自动同步

## v2.1.6

- 新增面包屑导航组件，替换原有单一页面名称显示，支持显示完整导航路径，点击可返回上级页面
- 账号管理页面添加列设置功能，支持自定义列显示顺序和宽度
- 实现账号管理密码加密存储，自动兼容旧数据，修改后自动更新为加密数据

## v2.1.5

- 修复管理控制台搜索功能，解决用户管理和网址管理页面搜索框输入一个字符就失去焦点的问题

## v2.1.4

- 完善 Tailwind 配置，新增自定义尺寸、渐变色、阴影等配置
- 扩展 CSS 变量，新增主题渐变色、Todo 分类颜色、模糊效果、透明度等变量
- 移除硬编码值，统一使用 CSS 变量和 Tailwind 类，提高代码可维护性
- 从组件中提取内联样式到主题文件，提升开发体验
- 修复管理控制台卡片样式，添加管理面板专用主题变量
- 确保所有组件遵循统一的设计系统规范

## v2.1.3

- 新增图标缓存功能，使用 Cache API 存储网站图标，减少重复网络请求
- 优化日志功能，修复级别过滤、添加缓冲队列、实现日志轮转和导入功能
- 完善服务层日志记录（PasswordService、AuthService）

## v2.1.2

- 引入 TanStack Query 进行数据获取和缓存管理
- 重构服务层，添加 AbortSignal 支持

## v2.1.1

- 修复日志窗口功能，完善日志记录系统

## v2.0.0

- 新增 OCR 文字识别、浮动窗口、记事本等功能

